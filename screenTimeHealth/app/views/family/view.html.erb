<div class="container">
	<div class="row">
		<h2><%= @family.surname %></h2>
	</div>
  <div class="row">
    <%= link_to 'Add', family_add_path, :class => 'btn btn-primary btn-lg' %>
  </div>
  <div class="row">
    <h3> Number of kids: <%= @kids.count %> </h3>
  </div>
  <div class="row">
    <% @kids.each do |kid, index| %>
      <div class="col-md-4">
        <a href="/kid/view?id=<%= kid.id %>" type="button"> 
          <div class="panel">
              <div class="panel-heading">
                  <h3 class="panel-title"><%= kid.name %></h3>
              </div>
              <div class="panel-body">
                <div class="text-center">
                  <input type="text" value="<%= @last_kid_session[kid.id] > kid.threshold ? kid.threshold : @last_kid_session[kid.id] %>" data-max="<%= kid.threshold %>" class="dial">
                </div>
              </div>
          </div>
        </a>
      </div>
    <% end %>
  </div>
  <div class="row">
  	<div class="col-md-12">
  		<div class="panel">
        <div class="panel-heading">
          Obesity distribution
        </div>
        <div class="panel-body">
          <%= high_chart("session_chart1", @chart[0]) %>
        </div>
      </div>
  	</div>
  	<div class="col-md-12">
  		<div class="panel">
        <div class="panel-heading">
          Obesity distribution
        </div>
        <div class="panel-body">
          <%= high_chart("session_chart2", @chart[1]) %>
        </div>
      </div>
  	</div>
  	<div class="col-md-12">
  		<div class="panel">
        <div class="panel-heading">
          Obesity distribution
        </div>
        <div class="panel-body">
          <%= high_chart("session_chart3", @chart[2]) %>
        </div>
      </div>
  	</div>
  	<div class="col-md-12">
  		<div class="panel">
        <div class="panel-heading">
          Obesity distribution
        </div>
        <div class="panel-body">
          <%= high_chart("session_chart4", @chart[3]) %>
        </div>
      </div>
  	</div>
  </div>
  
</div>